package com.example.DemoGraphQL.interceptor;

import org.springframework.stereotype.Component;

@Component
public class FilterConfig {

//	@Bean
	//	public FilterRegistrationBean<CustomRequestInterceptor> graphQLRequestFilter() {
	//		FilterRegistrationBean<CustomRequestInterceptor> registrationBean = new FilterRegistrationBean<>();
	//		registrationBean.setFilter(new CustomRequestInterceptor());
	//
	//		// Set the order to the lowest precedence
	//		registrationBean.setOrder(Ordered.LOWEST_PRECEDENCE);
	//
	//		// Add URL patterns for which the filter should be applied
	//		registrationBean.addUrlPatterns("/graphql"); // Adjust the URL pattern accordingly
	//
	//		return registrationBean;
	//	}
}
